using System.Collections.Generic;
using Creatures;



/*
container for current game state
 */

 class GameState {
    //  public List<Character> npcs; // structure for all npcs, enemies or npcs (if any)

     // dictionary to hash local npc id to database id
    //  public Character player;
     
     // dictionary to hash local item id to database id

     // every NPC get a database ID, it is used to access the nps stats.

     // get character from database

     // get item from database

     // query 'npcs' for npc, return a reference to it

 }